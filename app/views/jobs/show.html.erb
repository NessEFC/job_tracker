Title: <%= @job.title %>
<br>

Description: <%= @job.description %>
<br>

Level of Interest: <%= @job.level_of_interest %>
<br>

City: <%= @job.city %>
<br>

Company: <%= link_to @job.company.name, company_path(@job.company) %>
<br/><br/>

<strong>Comments: (<%= @job.comments.size %>)</strong>
<%= render partial: 'jobs/comment', collection: @job.comments.reverse %>
<%= render partial: 'comments/form' %><br/><br/>

<%= link_to "Edit Job", edit_job_path(@job) %>
<%= link_to "Delete", job_path(@job), method: :delete, data: {confirm: "Do you really wish to delete this job?"} %><br><br>
<%= link_to "<< Back to All #{@job.company.name} Jobs", company_path(@job.company) %>
