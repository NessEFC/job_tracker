All Jobs for <strong><%= @company.name %></strong><br><br>
<%= link_to "Create a New Job", new_company_job_path(@company) %>

<ul>
<% @company.jobs.each do |job| %>
  <li><%= link_to job.title, job_path(job) %> at <%= job.company.name %></li>
<% end %>
</ul>

<strong>Contacts:</strong></br>
<%= render partial: 'companies/contact', collection: @company.contacts %>
<%= render partial: 'contacts/form' %><br/><br/>

<%= link_to "<< Back to All Companies", companies_path %>
